import {CapacitorConfig} from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'np.afterwork.25keys',
  appName: '25keys',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
